#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve(){
     int x,y;
     cin >> x >> y;

    if(x < y) cout << 100 << endl;
    else cout << 0 << endl;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    
        solve();

    return 0;
}